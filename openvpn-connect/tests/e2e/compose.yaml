services:
  init:
    build:
      context: .
      dockerfile: Dockerfile.server
    command: /scripts/init.sh
    volumes:
      - vpn-data:/vpn
      - ./init.sh:/scripts/init.sh:ro

  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    command: openvpn --config /vpn/server.conf --log /dev/stdout
    depends_on:
      init:
        condition: service_completed_successfully
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    healthcheck:
      test: ["CMD-SHELL", "ss -lun | grep -q ':1194'"]
      interval: 1s
      timeout: 2s
      retries: 20
    volumes:
      - vpn-data:/vpn:ro

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    command:
      - sh
      - openvpn-connect/tests/e2e/test-matrix.sh
    depends_on:
      server:
        condition: service_healthy
    environment:
      OPENVPN_CONNECT_E2E_PROFILE: /vpn/client.ovpn
      OPENVPN_CONNECT_E2E_TIMEOUT_SECONDS: "45"
      OPENVPN_USERNAME: e2e
      OPENVPN_PASSWORD: binding-password
      OPENVPN_CONNECT_E2E_CA: /vpn/ca.crt
      OPENVPN_CONNECT_E2E_CERT: /vpn/client.crt
      OPENVPN_CONNECT_E2E_KEY: /vpn/client.key
      CARGO_HOME: /cargo
      CARGO_TARGET_DIR: /target
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ../../..:/workspace:ro
      - vpn-data:/vpn:ro
      - cargo-cache:/cargo
      - target-cache:/target

volumes:
  vpn-data:
  cargo-cache:
  target-cache:
